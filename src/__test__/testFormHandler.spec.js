const handleSubmit = require("../client/js/formHandler");

describe('Test handleSubmit functionality', () => {
    test('Testing the handleSubmit() function to return true', () => {
        expect(handleSubmit.handleSubmit).toBeDefined();

    });
});

